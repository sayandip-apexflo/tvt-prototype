{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://apexfabric.local/schemas/v1/surveillance-desired-state.json",
  "title": "ApexFabric Surveillance Desired State V1",
  "type": "object",
  "additionalProperties": false,
  "required": ["edge_id", "revision", "cameras"],
  "properties": {
    "edge_id": {"type": "string", "minLength": 1},
    "revision": {"type": "integer", "minimum": 1},
    "cameras": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["camera_id", "source", "solution_pack", "apps"],
        "properties": {
          "camera_id": {"type": "string", "minLength": 1},
          "source": {"type": "string", "pattern": "^file:/run/secrets/apexfabric/[A-Za-z0-9._-]+$"},
          "solution_pack": {"const": "surveillance"},
          "fps": {"type": "number", "exclusiveMinimum": 0},
          "apps": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"enum": ["reid", "face_recognition", "face_enrollment", "intrusion", "people_counting"]}
          },
          "config": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "zones": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "intrusion": {
                    "type": "array",
                    "minItems": 1,
                    "items": {"$ref": "#/$defs/zone"}
                  },
                  "face_recognition": {
                    "type": "array",
                    "minItems": 1,
                    "items": {"$ref": "#/$defs/identity_zone"},
                    "description": "Optional. Omit for whole-frame face matching. Configure when either (a) matches/misses should only count inside a restricted area, or (b) the zone pairs with another face_recognition zone at a gate to derive attendance direction. For (b), name each zone '<gate>_entry' / '<gate>_exit' — the control plane infers direction from which zone id a face_match/face_unmatched event's location.id names, there is no separate direction field."
                  }
                }
              },
              "lines": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "people_counting": {
                    "type": "array",
                    "minItems": 1,
                    "items": {"$ref": "#/$defs/countingLine"}
                  }
                }
              },
              "face_gallery": {
                "type": "object",
                "additionalProperties": false,
                "required": ["source", "revision"],
                "description": "Control-plane-owned identity gallery. The edge never extracts embeddings for enrollment or assigns a person_id itself; it only loads the bundle the control plane already built and pushes down through desired state.",
                "properties": {
                  "source": {
                    "type": "string",
                    "pattern": "^file:/run/secrets/apexfabric/face-gallery/[A-Za-z0-9._-]+\\.bundle$"
                  },
                  "revision": {"type": "integer", "minimum": 1}
                }
              }
            }
          }
        }
      }
    }
  },
  "$defs": {
    "point": {
      "type": "array",
      "prefixItems": [
        {"type": "number", "minimum": 0, "maximum": 1},
        {"type": "number", "minimum": 0, "maximum": 1}
      ],
      "items": false,
      "minItems": 2,
      "maxItems": 2
    },
    "zone": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "poly"],
      "properties": {
        "name": {"type": "string", "minLength": 1},
        "poly": {"type": "array", "minItems": 3, "items": {"$ref": "#/$defs/point"}}
      }
    },
    "identity_zone": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "poly"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "name": {"type": "string", "minLength": 1},
        "poly": {"type": "array", "minItems": 3, "items": {"$ref": "#/$defs/point"}}
      }
    },
    "countingLine": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "a", "b"],
      "properties": {
        "name": {"type": "string", "minLength": 1},
        "a": {"$ref": "#/$defs/point"},
        "b": {"$ref": "#/$defs/point"},
        "in_side": {"enum": ["left", "right"], "default": "right"}
      }
    }
  }
}
