import React,{useEffect,useMemo,useState} from 'react';
import {Activity,Bell,Boxes,Camera,ChevronDown,Cpu,FileText,History,LayoutDashboard,MapPin,Moon,Plus,Search,Settings,Sun,Video,X} from 'lucide-react';
import {api,Feed,formatIST,formatISTTime,useSite,useTheme} from './shared';
import {snapshotUrl} from './api';
import {EnrollmentPanel} from './enrollment';
import {CameraCreateModal,CameraManagePanel,mergeCameraLists,useTvtCameras} from './cameras';
import {AlertsBar} from './alerts';
import {SolutionsPage} from './solutions';
import {ClusterPage} from './cluster';
import {OperationsPage} from './operations';
import {ReportsPage} from './reports';
import {TvtSiteTab} from './settings';
import apexfloLockup from './assets/apexflo-lockup.svg';
import './customer.css';

const payload=e=>e?.payload||{},type=e=>payload(e).event_type||payload(e).type||'';
const cameraId=e=>e.camera_id||payload(e).camera_id||payload(e).camera?.id||payload(e).payload?.camera_id||payload(e).data?.camera_id||'';
const occurred=e=>e.occurred_at||new Date((e.received_at||0)*1000).toISOString(),body=e=>payload(e).payload||payload(e).data||payload(e);
const plate=e=>{const p=payload(e);return p.payload?.plate?.text||p.payload?.plate_text||p.data?.plate||p.plate_text||p.plate||p.text||''};
const isPlate=e=>/plate_read|anpr/.test(type(e));
const displayable=e=>isPlate(e),sameCamera=(e,c)=>[c.camera_id,c.name].filter(Boolean).some(x=>String(x).toLowerCase()===String(cameraId(e)).toLowerCase());
const primarySnapshot=e=>(e?.snapshots||[]).find(s=>!/(?:person|plate|vehicle|face|crop)[_.-]/i.test(s.source_url||''))||(e?.snapshots||[])[0];

function summarize(cameras,events){const out=Object.fromEntries(cameras.map(c=>[c.camera_id,{plates:[]}]));for(const e of [...events].sort((a,b)=>Date.parse(occurred(b))-Date.parse(occurred(a)))){const camera=cameras.find(c=>sameCamera(e,c)),m=camera&&out[camera.camera_id],when=Date.parse(occurred(e));if(!m)continue;if(isPlate(e)&&plate(e)&&m.plates.length<5)m.plates.push({text:plate(e),when,event:e})}return out}
function normalizedBoxes(event){const root=body(event),items=[];if(root.subject?.bbox||root.subject?.bounding_box)items.push(root.subject);for(const key of ['detections','subjects','objects','tracks'])for(const item of Array.isArray(root[key])?root[key]:[])if(item?.bbox||item?.bounding_box||item?.box)items.push(item);if(root.bbox||root.bounding_box||root.box)items.push(root);const rw=Number(root.frame_width||root.image_width||root.width),rh=Number(root.frame_height||root.image_height||root.height);return items.flatMap(item=>{const b=item.bbox||item.bounding_box||item.box;let x,y,w,h;if(Array.isArray(b)){[x,y,w,h]=b}else if(b?.x1!==undefined){x=b.x1;y=b.y1;w=Number(b.x2)-Number(b.x1);h=Number(b.y2)-Number(b.y1)}else{x=b?.x??b?.left;y=b?.y??b?.top;w=b?.width??(Number(b?.right)-Number(x));h=b?.height??(Number(b?.bottom)-Number(y))}if([x,y,w,h].some(v=>!Number.isFinite(Number(v))))return[];x=Number(x);y=Number(y);w=Number(w);h=Number(h);return[{x,y,w,h,pixel:x>1||y>1||w>1||h>1,iw:Number(item.frame_width||item.image_width)||rw,ih:Number(item.frame_height||item.image_height)||rh,label:item.type||item.label||item.class_name||item.class||type(event),confidence:item.confidence}]})}
function eventSummary(e){const t=type(e);if(isPlate(e))return plate(e)?`Plate ${plate(e)} read`:'Plate read';return t.replaceAll('_',' ')||'Analytics event'}
function activityCards(events,limit=Infinity){const groups=[];for(const event of [...events].filter(displayable).sort((a,b)=>Date.parse(occurred(b))-Date.parse(occurred(a)))){const group={key:event.event_id,events:[event],when:occurred(event)};groups.push(group)}return groups.slice(0,limit)}
const withGroup=card=>({...card.events.find(e=>snapshotUrl(primarySnapshot(e)))||card.events[0],_groupEvents:card.events});

export default function CustomerDashboard(){const {data,error,refresh}=useSite(),[theme,toggleTheme]=useTheme(),[page,setPage]=useState('Overview'),[selectedCamera,setSelectedCamera]=useState(null),[selectedEvent,setSelectedEvent]=useState(null);const {tvtCameras,reloadTvtCameras}=useTvtCameras();const apexCameras=data?.cameras||[],events=data?.events||[],cameras=useMemo(()=>mergeCameraLists(tvtCameras,apexCameras),[tvtCameras,apexCameras]),values=useMemo(()=>summarize(cameras,events),[cameras,events]),active=cameras.filter(c=>c.in_use),current=cameras.find(c=>c.camera_id===selectedCamera),navigate=name=>{setPage(name);setSelectedCamera(null)},onCameraChanged=async()=>{await reloadTvtCameras();await refresh()};return <><div className="customer-app"><aside className="cu-sidebar"><a className="cu-brand" href="/dashboard"><img src={apexfloLockup} alt="ApexFlo"/><small>{data?.site_id||'Edge site'}</small></a><div className="cu-site"><MapPin size={18}/><div><small>YOUR SITE</small><strong>{data?.site_id||'Edge site'}</strong></div></div><span className="cu-nav-label">WORKSPACE</span><nav><Nav name="Overview" page={page} Icon={LayoutDashboard} go={navigate}/><Nav name="Cameras" page={page} Icon={Video} go={navigate}/><Nav name="Solutions" page={page} Icon={Boxes} go={navigate}/><Nav name="Reports" page={page} Icon={FileText} go={navigate}/><Nav name="Cluster" page={page} Icon={Cpu} go={navigate}/><Nav name="Operations" label="Activity" page={page} Icon={History} go={navigate}/><Nav name="Search" page={page} Icon={Search} go={navigate}/><Nav name="Settings" page={page} Icon={Settings} go={navigate}/></nav><div className="cu-sidebar-end"><div className="cu-demo"><i/>Live edge data<small>Refreshes every 5 seconds</small></div></div></aside><div className="cu-workspace"><header className="cu-topbar"><span>Monitoring / <strong>{current?'Camera detail':page}</strong></span><div><span className="cu-connected"><i/>Site monitoring</span><button className="cu-theme-toggle" onClick={toggleTheme}>{theme==='dark'?<Sun size={18}/>:<Moon size={18}/>}</button><span className="cu-avatar">AF</span></div></header><div className="cu-layout"><main className="cu-main"><div className="cu-heading"><div><span className="cu-eyebrow">TVT SECURE</span><h1>{current?current.name:page}</h1><p>{current?`${current.camera_id} · Live camera analytics`:page==='Search'?'Find retained events from the past 24 hours.':page==='Settings'?'Make this workspace work for your site.':page==='Cameras'?'Every camera. One place to keep an eye on things.':page==='Solutions'?'Manage approved catalog images, camera assignments, and rollouts.':page==='Reports'?'Attendance and vehicle traffic derived from configured gate lines.':page==='Cluster'?'Node, workload, and storage health for this K3s box.':page==='Operations'?'A redacted, append-only view of management changes.':"What's happening across your site right now."}</p></div></div>{error&&<p className="cu-notice">Live data unavailable: {error} <button onClick={refresh}>Retry</button></p>}{!data?<div className="cu-empty">Connecting to this site…</div>:current?<CameraView camera={current} value={values[current.camera_id]} onEvent={setSelectedEvent} onChanged={onCameraChanged}/>:page==='Search'?<SearchPage cameras={cameras} onEvent={setSelectedEvent}/>:page==='Settings'?<SettingsPage site={data.site_id} cameras={cameras} onSaved={refresh}/>:page==='Cameras'?<CamerasPage cameras={cameras} values={values} onCamera={setSelectedCamera} onChanged={onCameraChanged}/>:page==='Solutions'?<SolutionsPage onChanged={onCameraChanged}/>:page==='Reports'?<ReportsPage/>:page==='Cluster'?<ClusterPage/>:page==='Operations'?<OperationsPage/>:<Overview cameras={active} values={values} events={events} onCamera={setSelectedCamera} onEvent={setSelectedEvent}/>}</main><AlertsBar/></div></div></div>{selectedEvent&&<EventDetail event={selectedEvent} camera={cameras.find(c=>sameCamera(selectedEvent,c))} onClose={()=>setSelectedEvent(null)}/>}</>}

function Nav({name,label=name,page,Icon,go}){return <button className={page===name?'active':''} onClick={()=>go(name)}><Icon size={18}/>{label}</button>}
function hasAnpr(camera){return camera?.tvt?.assignments?.some(a=>a.apps?.includes('anpr'))||false}
function Overview({cameras,values,events,onCamera,onEvent}){const plates=[...events].filter(isPlate).sort((a,b)=>Date.parse(occurred(b))-Date.parse(occurred(a))).slice(0,5);const anyAnpr=cameras.some(hasAnpr);return <><div className="cu-stats"><Stat Icon={Camera} value={cameras.length} label="Active cameras"/></div><CameraGrid cameras={cameras} values={values} onCamera={onCamera}/>{anyAnpr&&<Collapsible title="Last 5 plate reads" count={plates.length}><PlateList events={plates} onEvent={onEvent}/></Collapsible>}<Collapsible title="Last 10 camera events" count={Math.min(activityCards(events).length,10)}><RecentEvents events={events} cameras={cameras} onSelect={onEvent} limit={10} bare/></Collapsible></>}
function Stat({Icon,value,label}){return <div><span className="cu-stat-icon"><Icon size={19}/></span><small>{label}</small><strong>{String(value).padStart(typeof value==='number'?2:0,'0')}</strong><p>Latest platform data</p></div>}
function CameraGrid({cameras,values,showFeed,onCamera}){return <><div className="cu-section-title"><h2>Camera summaries <b>{cameras.length}</b></h2></div><div className="cu-camera-grid">{cameras.map(c=><CameraCard key={c.camera_id} camera={c} value={values[c.camera_id]} showFeed={showFeed} onClick={()=>onCamera(c.camera_id)}/>)}</div></>}
function CamerasPage({cameras,values,onCamera,onChanged}){const [showCreate,setShowCreate]=useState(false);return <><div className="cu-section-title"><h2>All cameras <b>{cameras.length}</b></h2><button className="cu-btn cu-primary" onClick={()=>setShowCreate(true)}><Plus size={15}/>Add camera</button></div><div className="cu-camera-grid">{cameras.map(c=><CameraCard key={c.camera_id} camera={c} value={values[c.camera_id]} showFeed onClick={()=>onCamera(c.camera_id)}/>)}</div>{!cameras.length&&<div className="cu-empty">No cameras yet. Add one to get started.</div>}{showCreate&&<CameraCreateModal close={()=>setShowCreate(false)} onCreated={onChanged}/>}</>}
function CameraCard({camera,value={},showFeed,onClick}){return <button className={`cu-camera-card ${showFeed?'':'cu-simple-camera'}`} onClick={onClick}>{showFeed&&(camera.in_use?<Feed cameraId={camera.camera_id}/>:<div className="cu-empty">Not deployed yet</div>)}<div className="cu-camera-info"><span className="cu-simple-icon"><Camera size={22}/></span><div><h3>{camera.name||camera.camera_id}</h3><span>{camera.camera_id}</span></div><span className={camera.in_use?'cu-running':'cu-pending'}><i/>{camera.in_use?'Running':camera.tvt?'Configured':'Unmanaged'}</span></div></button>}
function Collapsible({title,count,children}){return <details className="cu-collapsible" open><summary><span>{title} <b>{count}</b></span><ChevronDown size={17}/></summary>{children}</details>}
function PlateList({events,onEvent}){return events.length?<ol className="cu-plate-list">{events.map((e,i)=><li key={e.event_id||i}><button onClick={()=>onEvent(e)}><strong>{plate(e)||'Unknown plate'}</strong><span>{cameraId(e)||'Unknown camera'}</span><time>{formatIST(occurred(e))}</time></button></li>)}</ol>:<div className="cu-empty">No plate reads received.</div>}
function CameraView({camera,value={},onEvent,onChanged}){const [tab,setTab]=useState('Live');return <><div className="cu-tabs cu-settings-tabs">{['Live','Manage'].map(t=><button key={t} className={tab===t?'active':''} onClick={()=>setTab(t)}>{t}</button>)}</div>{tab==='Live'&&<><section className="cu-live-panel"><div className="cu-section-title"><h2>Live feed</h2><span><i/>Fresh frame every 5 seconds</span></div><Feed cameraId={camera.camera_id}/></section><EnrollmentPanel camera={camera}/>{hasAnpr(camera)&&<Collapsible title="Last 5 plate reads" count={value.plates?.length||0}><PlateList events={(value.plates||[]).map(x=>x.event)} onEvent={onEvent}/></Collapsible>}<PagedCameraEvents camera={camera} onEvent={onEvent}/></>}{tab==='Manage'&&<CameraManagePanel cameraId={camera.camera_id} friendlyName={camera.name} onChanged={onChanged}/>}</>}
function PagedCameraEvents({camera,onEvent}){const [page,setPage]=useState(0),[result,setResult]=useState({events:[],has_more:false}),[error,setError]=useState(''),size=10;useEffect(()=>{let live=true;setError('');api(`telemetry/events?camera_id=${encodeURIComponent(camera.camera_id)}&limit=${size}&offset=${page*size}`).then(x=>live&&setResult(x)).catch(e=>live&&setError(e.message));return()=>{live=false}},[camera.camera_id,page]);return <section className="cu-recent-events"><div className="cu-section-title"><h2>Camera analytics events</h2><span>Page {page+1}</span></div>{error?<p className="cu-notice">{error}</p>:<RecentEvents events={result.events} cameras={[camera]} onSelect={onEvent} bare/>}<Pagination page={page} setPage={setPage} hasMore={result.has_more}/></section>}
function Pagination({page,setPage,hasMore}){return <div className="cu-pagination"><button disabled={!page} onClick={()=>setPage(p=>p-1)}>Previous</button><span>Page {page+1}</span><button disabled={!hasMore} onClick={()=>setPage(p=>p+1)}>Next</button></div>}
function RecentEvents({events,cameras,onSelect,limit=Infinity,bare=false}){const cards=activityCards(events,limit),content=<div className="cu-event-list cu-activity-list">{cards.map(card=>{const evidence=withGroup(card),camera=cameras.find(c=>sameCamera(evidence,c));return <button key={card.key} onClick={()=>onSelect(evidence)}><span className="cu-event-icon"><Activity size={17}/></span><div><strong>{eventSummary(evidence)}</strong><small>{camera?.name||cameraId(evidence)||'Unknown camera'}{snapshotUrl(primarySnapshot(evidence))?' · View snapshot':' · Event received'}</small></div><time>{formatISTTime(card.when)}</time></button>})}{!cards.length&&<div className="cu-empty">No analytics events received.</div>}</div>;return bare?content:<section className="cu-recent-events"><div className="cu-section-title"><h2>Camera activity <b>{cards.length}</b></h2></div>{content}</section>}
function SearchPage({cameras,onEvent}){const [filters,setFilters]=useState({q:'',event_type:'',camera_id:'',start:'',end:''}),[submitted,setSubmitted]=useState(filters),[page,setPage]=useState(0),[result,setResult]=useState({events:[],has_more:false}),[error,setError]=useState(''),size=25;useEffect(()=>{let live=true;const params=new URLSearchParams({limit:String(size),offset:String(page*size)});for(const [key,value] of Object.entries(submitted))if(value)params.set(key,key==='start'||key==='end'?String(new Date(value).getTime()/1000):value);api(`telemetry/events?${params}`).then(x=>live&&setResult(x)).catch(e=>live&&setError(e.message));return()=>{live=false}},[submitted,page]);return <><form className="cu-search-form" onSubmit={e=>{e.preventDefault();setPage(0);setSubmitted(filters)}}><label>Plate or event text<input value={filters.q} onChange={e=>setFilters({...filters,q:e.target.value})} placeholder="e.g. KA01AB1234"/></label><label>Event type<select value={filters.event_type} onChange={e=>setFilters({...filters,event_type:e.target.value})}><option value="">All current events</option><option value="plate_read">Plate reads</option></select></label><label>Camera<select value={filters.camera_id} onChange={e=>setFilters({...filters,camera_id:e.target.value})}><option value="">All cameras</option>{cameras.map(c=><option key={c.camera_id} value={c.camera_id}>{c.name}</option>)}</select></label><label>From<input type="datetime-local" value={filters.start} onChange={e=>setFilters({...filters,start:e.target.value})}/></label><label>To<input type="datetime-local" value={filters.end} onChange={e=>setFilters({...filters,end:e.target.value})}/></label><button className="cu-btn cu-primary"><Search size={15}/>Search events</button></form>{error?<p className="cu-notice">{error}</p>:<RecentEvents events={result.events} cameras={cameras} onSelect={onEvent}/>}<Pagination page={page} setPage={setPage} hasMore={result.has_more}/></>}
function SettingsPage({site,cameras,onSaved}){const [tab,setTab]=useState('Device Settings'),[siteName,setSiteName]=useState(site||''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');useEffect(()=>setSiteName(site||''),[site]);async function saveSite(e){e.preventDefault();setBusy(true);setMessage('');try{await api('site',{site_id:siteName});setMessage('Site name saved.');await onSaved()}catch(error){setMessage(error.message)}finally{setBusy(false)}}return <><div className="cu-tabs cu-settings-tabs">{['Device Settings','Site & Security','Alerts Config'].map(name=><button key={name} className={tab===name?'active':''} onClick={()=>setTab(name)}>{name}</button>)}</div>{tab==='Device Settings'?<><section className="cu-settings-panel"><h2>Site details</h2><p>The customer-facing identity for this monitored edge site.</p><form className="cu-site-form" onSubmit={saveSite}><label>Site name<input value={siteName} onChange={e=>setSiteName(e.target.value)} required maxLength="80"/></label><button className="cu-btn cu-primary" disabled={busy||siteName.trim()===site}>{busy?'Saving…':'Save site name'}</button></form>{message&&<p className="cu-site-message" role="status">{message}</p>}</section><section className="cu-settings-panel"><div className="cu-section-title"><h2>Cameras <b>{cameras.length}</b></h2><span>Managed by your site administrator</span></div>{cameras.map(c=><div className="cu-device-row" key={c.camera_id}><Camera size={20}/><div><strong>{c.name||c.camera_id}</strong><small>{c.camera_id} · {c.assigned_to?.join(', ')||'No deployment assigned'}</small></div><span className={c.in_use?'cu-running':'cu-pending'}>{c.in_use?'Monitoring':'Pending deployment'}</span></div>)}</section></>:tab==='Site & Security'?<TvtSiteTab/>:<section className="cu-settings-panel cu-alert-placeholder"><span className="cu-stat-icon"><Bell size={20}/></span><h2>Alert configuration</h2><p>The alerts layer is not enabled yet. This space is reserved for customer alert rules, thresholds, notification routing, and schedules.</p></section>}</>}
function EventDetail({event,camera,onClose}){const snapshot=snapshotUrl(primarySnapshot(event)),detections=normalizedBoxes(event),[size,setSize]=useState(null),[imageError,setImageError]=useState(false);return <div className="cu-event-scrim" onClick={onClose}><article className="cu-event-modal" onClick={e=>e.stopPropagation()}><div className="cu-event-modal-head"><div><span className="cu-eyebrow">EVENT EVIDENCE</span><h2>{eventSummary(event)}</h2><p>{camera?.name||cameraId(event)} · {formatIST(occurred(event))}</p></div><button onClick={onClose} aria-label="Close event evidence"><X/></button></div>{snapshot&&!imageError?<div className="cu-evidence-frame" style={{aspectRatio:size?`${size.width}/${size.height}`:'16/9'}}><img src={snapshot} alt={`${eventSummary(event)} snapshot`} onLoad={e=>setSize({width:e.currentTarget.naturalWidth,height:e.currentTarget.naturalHeight})} onError={()=>setImageError(true)}/>{size&&detections.map((d,i)=>{const iw=d.iw||size.width,ih=d.ih||size.height,x=d.pixel?d.x/iw*100:d.x*100,y=d.pixel?d.y/ih*100:d.y*100,w=d.pixel?d.w/iw*100:d.w*100,h=d.pixel?d.h/ih*100:d.h*100;return <span key={i} className="cu-bounding-box" style={{left:`${Math.max(0,x)}%`,top:`${Math.max(0,y)}%`,width:`${Math.max(0,Math.min(100-x,w))}%`,height:`${Math.max(0,Math.min(100-y,h))}%`}}><b>{d.label}{Number.isFinite(+d.confidence)?` · ${Math.round(+d.confidence*100)}%`:''}</b></span>})}</div>:<div className="cu-empty">{imageError?'The retained snapshot could not be loaded.':'This event does not include a retained snapshot.'}</div>}<dl className="cu-event-facts"><div><dt>Camera</dt><dd>{camera?.name||cameraId(event)||'—'}</dd></div><div><dt>Event</dt><dd>{eventSummary(event)}</dd></div></dl></article></div>}
