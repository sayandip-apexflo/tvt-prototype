# Optional credentials for the pinned PIPELINE Git/LFS fetch. Leave these
# commented for a public repository. The installed file is root-only (0600).
# PIPELINE_GIT_USERNAME=x-access-token
# PIPELINE_GITHUB_TOKEN=replace-with-a-read-only-token

