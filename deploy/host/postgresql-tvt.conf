# TVT management PostgreSQL is a host-local service. Applications connect
# through the Unix socket using peer authentication.
listen_addresses = ''
password_encryption = 'scram-sha-256'
log_connections = on
log_disconnections = on
